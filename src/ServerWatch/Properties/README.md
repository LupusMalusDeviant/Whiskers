# Properties

Project metadata for local development.

## Files

| File | Purpose |
|---|---|
| `launchSettings.json` | Local run profiles (URLs, environment) used by `dotnet run` / the IDE. Not used in the Docker/production deployment. |
