@page "/approvals"
@attribute [Authorize]

@* Route wrapper (DI-safe module gating). ApprovalsView injects the approval store, so it is rendered only while
   the Agent module is enabled; otherwise ModuleGuard shows a "module disabled" notice. *@
<ModuleGuard ModuleId="agent">
    <ApprovalsView />
</ModuleGuard>
